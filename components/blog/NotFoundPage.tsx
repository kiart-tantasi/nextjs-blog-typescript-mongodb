import styles from "./NotFoundPage.module.css";

const NotFoundPage = () => {
  return (
    <div className={styles.container}>
      <p>ไม่พบหน้าที่ต้องการ (404)</p>
    </div>
  );
};

export default NotFoundPage;
